<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!--  日志格式  -->
    <property name="LOG_PATTERN"
              value="%date{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n" />

    <!--  文件存储路径  -->
    <!--  WINDOWS  -->
    <property name="FILE_PATH"
              value="E:/_WORKSPACE/IDEA/XNOTE/logs/manage_%d{yyyy-MM-dd}_%i.log" />
    <!--  LINUX  -->
    <!--
    <property name="FILE_PATH"
    value="/usr/local/server/XNOTE_server/logs/manage_log/manage_%d{yyyy-MM- dd}(%i).log" />
    -->

    <!--  定义控制台输出  -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <!-- 按照上面配置的LOG_PATTERN来打印日志 -->
            <pattern>${LOG_PATTERN}</pattern>
        </encoder>
    </appender>

    <!--  定义日志文件的相关参数  -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">

            <!-- 按照上面配置的FILE_PATH路径来保存日志 -->
            <fileNamePattern>${FILE_PATH}</fileNamePattern>

            <!-- 日志保存15天 -->
            <maxHistory>15</maxHistory>
            <timeBasedFileNamingAndTriggeringPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedFNATP">
                <!-- 单个日志文件的最大，超过则新建日志文件存储 -->
                <maxFileSize>10MB</maxFileSize>
            </timeBasedFileNamingAndTriggeringPolicy>
        </rollingPolicy>

        <!-- 按照上面配置的LOG_PATTERN来打印日志 -->
        <encoder>
            <pattern>${LOG_PATTERN}</pattern>
        </encoder>
    </appender>

    <!--  定义日志输出级别  -->
<!--    <logger name="com.xnote.manage.modules.**.service" level="INFO" />-->com.xnote.manage.log
    <logger name="com.xnote.manage.modules.**.service.impl" level="INFO" />
    <root level="INFO">
        <appender-ref ref="CONSOLE" />
        <appender-ref ref="FILE" />
    </root>
</configuration>